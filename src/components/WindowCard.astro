---
interface Props {
	path?: string;
	class?: string;
}

const { path = '~', class: className = '' } = Astro.props;
---

<div
	class:list={[
		'relative overflow-hidden rounded-lg border border-border bg-bg-soft shadow-[0_0_0_1px_rgba(91,103,255,0.04)]',
		className,
	]}
>
	<div
		class="flex items-center justify-between border-b border-border px-4 py-2.5 font-mono text-2xs text-fg-subtle"
	>
		<span class="flex items-center gap-1.5" aria-hidden="true">
			<span class="h-2 w-2 rounded-full bg-negative"></span>
			<span class="h-2 w-2 rounded-full bg-warning"></span>
			<span class="h-2 w-2 rounded-full bg-positive"></span>
		</span>
		<span class="tracking-wider uppercase">{path}</span>
	</div>
	<slot />
</div>
